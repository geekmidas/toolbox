TEST_VAR=1
# EXAMPLE_URL is deliberately absent: the `Example` database construct declares
# it, and `gkm dev` / `gkm exec` inject it from the container they reconciled.
# Restating it here would override the port that was actually allocated.
