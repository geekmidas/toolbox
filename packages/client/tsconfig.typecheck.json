{
	"extends": "./tsconfig.json",
	"compilerOptions": {
		"noEmit": true
	},
	"include": ["src/**/*"],
	"//": "The runtime specs stay out. They were never compiled — the build tsconfig excludes the whole test directory — and compiling them now reports 60 pre-existing errors that have nothing to do with the types being asserted here. Worth fixing; not worth blocking every type assertion behind.",
	"exclude": ["src/__tests__/**/*.spec.ts", "src/__tests__/**/*.spec.tsx"]
}
